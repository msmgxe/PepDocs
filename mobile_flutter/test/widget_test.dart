import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — full integration tests require a live Supabase instance.
    expect(true, isTrue);
  });
}
